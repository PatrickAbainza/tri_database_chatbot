// Define your handlers here. Example:
// export const handlers = [
//   http.get('/api/user', () => {
//     return HttpResponse.json({ firstName: 'John', lastName: 'Maverick' })
//   }),
// ]

// For now, export an empty array if no handlers are needed initially
export const handlers = [];
